# No environment variables are required to build or run this site.
# The contact-form key lives in content/settings.json (contact.accessKey),
# not here, so a non-developer can set it through /admin.
